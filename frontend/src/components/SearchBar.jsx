import React from "react";

const SearchBar = (props) => {
    return (
        <form>
            <input type="text" />
            <button>Search</button>
        </form>
    );
};

export default SearchBar;
